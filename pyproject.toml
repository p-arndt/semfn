[project]
name = "semfn"
version = "0.1.0"
description = "Semantic, probabilistic Python functions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["laya>=0.3.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
